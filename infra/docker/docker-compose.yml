version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: egocentric_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: egocentric
      POSTGRES_USER: egocentric_user
      POSTGRES_PASSWORD: egocentric_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../packages/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U egocentric_user -d egocentric"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: egocentric_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  pipeline:
    build:
      context: ../../services/pipeline
      dockerfile: Dockerfile
    container_name: egocentric_pipeline
    restart: unless-stopped
    env_file:
      - ../../.env
    ports:
      - "8000:8000"
    environment:
      REDIS_URL: redis://redis:6379/0
      DATABASE_URL: postgresql://egocentric_user:egocentric_pass@postgres:5432/egocentric
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
